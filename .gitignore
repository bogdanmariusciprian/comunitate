# Environment / secrets — never commit real keys
.env
.env.local
*.local

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Dependencies (in case tooling is added later)
node_modules/

# Logs
*.log

# Private project notes (Supabase schema reference) — local only
SUPABASE_SCHEMA.md
